import { legacyForm } from "../legacy.js";
export const descriptor = Object.freeze({ familyId: "flow", memberId: "sankey", fixtureId: "flow/sankey/fixture-v1", incumbent: true });
export default legacyForm(descriptor);
