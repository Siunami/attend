import { legacyForm } from "../legacy.js";
export const descriptor = Object.freeze({ familyId: "point-map", memberId: "exact-points", fixtureId: "point-map/exact-points/fixture-v1", incumbent: true, assets: ["vendor/d3.min.js", "vendor/topojson-client.min.js", "vendor/world-countries.json"] });
export default legacyForm(descriptor);
