import { legacyForm } from "../legacy.js";
export const descriptor = Object.freeze({ familyId: "timeline", memberId: "interval", fixtureId: "timeline/interval/fixture-v1", incumbent: true });
export default legacyForm(descriptor);
