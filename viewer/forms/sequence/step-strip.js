import { legacyForm } from "../legacy.js";
export const descriptor = Object.freeze({ familyId: "sequence", memberId: "step-strip", fixtureId: "sequence/step-strip/fixture-v1", incumbent: true });
export default legacyForm(descriptor);
