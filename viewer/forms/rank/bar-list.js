import { legacyForm } from "../legacy.js";
export const descriptor = Object.freeze({ familyId: "rank", memberId: "bar-list", fixtureId: "rank/bar-list/fixture-v1", incumbent: true });
export default legacyForm(descriptor);
