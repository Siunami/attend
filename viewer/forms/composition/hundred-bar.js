import { legacyForm } from "../legacy.js";
export const descriptor = Object.freeze({ familyId: "composition", memberId: "hundred-bar", fixtureId: "composition/hundred-bar/fixture-v1", incumbent: true });
export default legacyForm(descriptor);
