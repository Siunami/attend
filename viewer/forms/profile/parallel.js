import { legacyForm } from "../legacy.js";
export const descriptor = Object.freeze({ familyId: "profile", memberId: "parallel", fixtureId: "profile/parallel/fixture-v1", incumbent: true });
export default legacyForm(descriptor);
