import { legacyForm } from "../legacy.js";
export const descriptor = Object.freeze({ familyId: "trend", memberId: "line", fixtureId: "trend/line/fixture-v1", incumbent: true });
export default legacyForm(descriptor);
