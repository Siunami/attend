import { legacyForm } from "../legacy.js";
export const descriptor = Object.freeze({ familyId: "hierarchy", memberId: "tidy", fixtureId: "hierarchy/tidy/fixture-v1", incumbent: true });
export default legacyForm(descriptor);
