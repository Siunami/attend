import { legacyForm } from "../legacy.js";
export const descriptor = Object.freeze({ familyId: "passage-comparison", memberId: "parallel-text", fixtureId: "passage-comparison/parallel-text/fixture-v1", incumbent: true });
export default legacyForm(descriptor);
