import { legacyForm } from "../legacy.js";
export const descriptor = Object.freeze({ familyId: "matrix", memberId: "heatmap", fixtureId: "matrix/heatmap/fixture-v1", incumbent: true });
export default legacyForm(descriptor);
