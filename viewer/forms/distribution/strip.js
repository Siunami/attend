import { legacyForm } from "../legacy.js";
export const descriptor = Object.freeze({ familyId: "distribution", memberId: "strip", fixtureId: "distribution/strip/fixture-v1", incumbent: true });
export default legacyForm(descriptor);
