import { legacyForm } from "../legacy.js";
export const descriptor = Object.freeze({ familyId: "network", memberId: "local", fixtureId: "network/local/fixture-v1", incumbent: true });
export default legacyForm(descriptor);
