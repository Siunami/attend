import { legacyForm } from "../legacy.js";
export const descriptor = Object.freeze({ familyId: "field", memberId: "sample-raster", fixtureId: "field/sample-raster/fixture-v1", incumbent: true });
export default legacyForm(descriptor);
