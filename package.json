{
  "name": "attend-local",
  "version": "0.2.0",
  "description": "Local, agent-native visualization harness for question-shaped views over private work.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Siunami/attend-local.git"
  },
  "type": "module",
  "bin": {
    "attend": "bin/attend.js"
  },
  "files": [
    "agent-skill/",
    "bin/",
    "src/",
    "viewer/",
    "README.md"
  ],
  "scripts": {
    "test": "node --test",
    "verify": "node --test && npm pack --dry-run"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "license": "UNLICENSED"
}
